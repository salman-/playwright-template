# playwright-template

### How to run the tests ?

To run the test in headless mode execute: `npm run headless-test`
To run the test in headed mode execute: `npm run headed-test`

### How to do type-check ?

npx tsc --noEmit